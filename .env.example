# Production database password (used in config/database.yml)
MAP_DATABASE_PASSWORD=

# React build on server (Capistrano reads shared/.env)
REACT_APP_MAPBOX_ACCESS_TOKEN=

# Optional monitoring
SENTRY_DSN=
