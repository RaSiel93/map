# Rails API URL for development (CRA runs on a separate port)
REACT_APP_API_URL=http://localhost:3000

# Mapbox token — required for the map to render
REACT_APP_MAPBOX_ACCESS_TOKEN=

# CRA dev server port (must differ from Rails on :3000; CORS allows :3001)
PORT=3001
