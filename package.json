{
  "scripts": {
    "dev": "bin/dev",
    "start": "yarn --cwd client start",
    "build": "yarn --cwd client --max_old_space_size=4096 build",
    "lint": "yarn --cwd client lint",
    "deploy": "cp -a client/build/. public/",
    "deploy:production": "bin/deploy",
    "deploy:backend": "bin/deploy-backend"
  },
  "license": "MIT",
  "dependencies": {
    "@deck.gl/react": "^9.0.38",
    "lodash": "^4.17.21"
  }
}
